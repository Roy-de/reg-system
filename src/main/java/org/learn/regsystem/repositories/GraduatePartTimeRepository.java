package org.learn.regsystem.repositories;

import org.learn.regsystem.entities.GraduatePartTime;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GraduatePartTimeRepository extends JpaRepository<GraduatePartTime, Long> {
}