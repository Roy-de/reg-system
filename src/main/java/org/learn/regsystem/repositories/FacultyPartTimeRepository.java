package org.learn.regsystem.repositories;

import org.learn.regsystem.entities.FacultyPartTime;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FacultyPartTimeRepository extends JpaRepository<FacultyPartTime, Long> {
}